1.include指令：

  a.是一种静态导入指令，即完完全全的代码嵌入，和C语言的include指令完全一样

  b.<%@include file="要包含的jsp文件">

  c.该指令可以放在当前jsp页面的任意位置，会在嵌入位置处完全将另一个jsp页面的代码完整嵌入到该位置

  d.注意：由于是简单的完全嵌入，因此也会将另一个页面的所有编译指令也嵌入，如果和当前页面编译指令冲突则会报错！

  e.其效果就是将两个jsp页面完全融合成一个jsp页面，生成的Servlet也是只有一个不会有两个